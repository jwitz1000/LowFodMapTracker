import React from "react";

const Team = (props) => {
  return (
    <div style={{ color: `green` }}>
      <div style={{ color: `red` }}>Team</div>
      <div>page</div>
    </div>
  );
};

export default Team;
